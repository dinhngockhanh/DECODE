% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DECODE_plots.r
\name{DECODE_plot_SFS}
\alias{DECODE_plot_SFS}
\title{Plot empirical and fitted Site Frequency Spectra}
\usage{
DECODE_plot_SFS(
  DECODE_result,
  filename = NULL,
  filetype = "png",
  width = 30,
  height = 15,
  units = "in",
  res = 150,
  with_tail = NA,
  N_clusters = NULL,
  mode = "inference_A",
  DECODE_linewidth = 5,
  text_xlab = "Variant Allele Frequency",
  color_xlab = "black",
  text_ylab = "Mutation count",
  notation = TRUE,
  x_min = NULL,
  x_max = NULL,
  data_marker_colors = NULL,
  error_bar = TRUE
)
}
\arguments{
\item{DECODE_result}{Object returned by \code{\link{DECODE}}.}

\item{filename}{Character path prefix for saving the plot. If \code{NULL}
(default), a \code{ggplot} object is returned.}

\item{filetype}{Output format when \code{filename} is supplied: \code{"png"}
(default), \code{"jpg"}, \code{"svg"}, \code{"tiff"}, \code{"eps"}, or
\code{"pdf"}.}

\item{width}{Plot width when saving to file (\code{30} by default).}

\item{height}{Plot height when saving to file (\code{15} by default).}

\item{units}{Units for \code{width} and \code{height} (\code{"in"} by default).}

\item{res}{Resolution in dpi when saving raster formats (\code{150} by default).}

\item{with_tail}{Logical indicating whether to plot a fit with
(\code{TRUE}) or without (\code{FALSE}) a neutral tail. If \code{NA}
(default), the best overall fit from \code{\link{DECODE}} is used.}

\item{N_clusters}{Integer cluster count for the fit to plot. If \code{NULL}
(default), the best cluster count from \code{\link{DECODE}} is used.}

\item{mode}{Subsample to display: \code{"inference_A"} (default),
\code{"inference_B"}, or \code{"validation"}.}

\item{DECODE_linewidth}{Line width for fitted component curves (\code{5} by default).}

\item{text_xlab}{X-axis label; \code{NULL} suppresses the axis label.}

\item{color_xlab}{Color of the x-axis label (\code{"black"} by default).}

\item{text_ylab}{Y-axis label; \code{NULL} suppresses the y-axis label and ticks.}

\item{notation}{Logical; include title, legend, and axis annotations
(\code{TRUE} by default).}

\item{x_min}{Lower x-axis limit. If \code{NULL} (default), chosen automatically.}

\item{x_max}{Upper x-axis limit. If \code{NULL} (default), chosen automatically.}

\item{data_marker_colors}{Named character vector of colors for empirical SFS
bars. Defaults to black for \code{"Data"}. If \code{mutation_table} contains
a \code{Marker} column, one color per marker can be supplied.}

\item{error_bar}{Logical; draw uncertainty ribbons around the fitted SFS
(\code{TRUE} by default).}
}
\value{
If \code{filename} is \code{NULL}, a \code{ggplot} object. Otherwise
  the plot is written to disk and nothing is returned invisibly.
}
\description{
Compares the empirical SFS against the DECODE deconvolution for a chosen
subsample and model configuration.
}
