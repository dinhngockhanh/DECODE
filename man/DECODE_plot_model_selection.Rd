% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DECODE_plots.r
\name{DECODE_plot_model_selection}
\alias{DECODE_plot_model_selection}
\title{Plot DECODE model selection diagnostics}
\usage{
DECODE_plot_model_selection(
  DECODE_result,
  filename = NULL,
  filetype = "png",
  max_variant_read = 50,
  max_total_read = 150,
  width = 30,
  height = 15,
  units = "in",
  res = 150,
  data_marker_colors = NULL
)
}
\arguments{
\item{DECODE_result}{Object returned by \code{\link{DECODE}}.}

\item{filename}{Character path prefix for saving the plot. If \code{NULL}
(default), the plot object is returned instead of being saved.}

\item{filetype}{Output format when \code{filename} is supplied: \code{"png"}
(default), \code{"jpg"}, \code{"svg"}, \code{"tiff"}, \code{"eps"}, or
\code{"pdf"}.}

\item{max_variant_read}{Maximum variant read count shown in the read-count
heatmap (\code{50} by default).}

\item{max_total_read}{Maximum total read count shown in the read-count
heatmap (\code{150} by default).}

\item{width}{Plot width when saving to file (\code{30} by default).}

\item{height}{Plot height when saving to file (\code{15} by default).}

\item{units}{Units for \code{width} and \code{height} (\code{"in"} by default).}

\item{res}{Resolution in dpi when saving raster formats (\code{150} by default).}

\item{data_marker_colors}{Named character vector of colors for empirical SFS
markers in the fit panels. Defaults to black for \code{"Data"}.}
}
\value{
If \code{filename} is \code{NULL}, a \code{grid} object. Otherwise
  the plot is written to disk and nothing is returned invisibly.
}
\description{
Creates a composite figure summarizing mutation threshold selection,
model-selection criterion values, and SFS fits for all tail and cluster-count
configurations considered by \code{\link{DECODE}}.
}
